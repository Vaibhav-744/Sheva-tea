/* =============================================================================
   Sheva Tea — Custom JS
   Loaded by theme.liquid when settings.use_custom_js = true.
   The two core Sheva scripts are already loaded via <script defer> tags
   in theme.liquid just before </body>, so this file is a safety net only.
============================================================================= */

/* Nothing needed here — sheva-sliders.js and sheva-product.js are loaded
   directly in layout/theme.liquid via defer script tags.
   Add any store-specific JS overrides below. */
